#include <bits/stdc++.h>

int main(){
	long n,k;
	cin >> n >>k;
	long i;
	long* m=(long*) calloc(4000,sizeof(long));
	long* c=(long*) calloc(4000,sizeof(long));
	for(i=0;i<n;i++){
		
		
	}
	
}
